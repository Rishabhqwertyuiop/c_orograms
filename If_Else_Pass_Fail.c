int main() {
    int num;

    printf("Enter a num: ");
    scanf("%d", &num);

    if (num >= 40) {
        printf("Person is Pass");
    } else {
        printf("Person is fail");
    }
    return 0;
}